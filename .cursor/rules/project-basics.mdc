---
description: TraceEvolve project conventions, commands, canonical files, and prohibitions
alwaysApply: true
---

# TraceEvolve — 经验提取与管理系统

## 项目用途

从 QiMeng-Agent（Verilog 生成 Agent）的 JSON 运行日志中提取可复用的编程经验，
维护去重、合并后的经验池（`experience_pool.json`），导出 ICL 文本供 Agent 回注。

## 目录结构

```
trace_evolve/
  config.py        # dataclass 配置 + 所有 prompt 模板
  extractor.py     # Experience/TaskSegment/LogParser/QiMengLogParser/LLMClient/ExperienceExtractor
  manager.py       # ExperiencePool/ExperienceManager（候选级 LLM merge + 规则预判）
  postprocessor.py # ExperiencePostProcessor：归一化、质量过滤、组内去重
  quality.py       # quality_score(exp)：经验质量打分
  pipeline.py      # EvolvePipeline：串联 extract → postprocessor → merge → save
  cli.py           # CLI 入口 (python -m trace_evolve.cli)
  utils.py         # 纯工具函数（ID 生成、截断、JSON IO、MockLLMClient、calculate_similarity）
  examples.py      # 示例脚本
```

## 入口与主流程

1. **CLI 入口**: `python -m trace_evolve.cli --files <log.json> --qimeng --pool experience_pool.json`
2. **数据流**: 日志文件 → `QiMengLogParser.segment_tasks()` → `TaskSegment` 列表
   → `ExperienceExtractor._extract_qimeng_per_segment()`（并发 LLM 调用）
   → 候选 `Experience` 列表 → `ExperiencePostProcessor.process()`（归一化/过滤/去重）
   → `ExperienceManager.merge_experiences()`（候选级 merge：规则预判 + LLM）
   → `ExperiencePool.save()`
3. **Python API**: `EvolvePipeline(config).process_log_files(files, use_qimeng_parser=True)`

## 关键命令

```bash
# 提取 QiMeng-Agent 日志
python -m trace_evolve.cli --files /path/to/run_*.json --qimeng --pool experience_pool.json

# 处理日志目录（QiMeng 模式会优先匹配 run_*.json）
python -m trace_evolve.cli --dir /path/to/qimeng/logs --qimeng --pool experience_pool.json

# 导出 ICL 经验
python -m trace_evolve.cli --export --pool experience_pool.json --output experiences.txt
```

## 依赖

- Python 3.8+
- `openai`（OpenAI 兼容 API）
- `volcenginesdkarkruntime`（可选，火山引擎 Ark）

## 代码风格与模式

- 所有 dataclass 放在各自模块顶层（`Experience`、`TaskSegment` 在 extractor.py，`Operation` 在 manager.py）
- Prompt 模板全部集中在 `config.py`，用 `{variable}` 占位；JSON 花括号用 `{{` `}}` 转义
- LLM 调用统一走 `LLMClient.call(prompt)`，返回 `str`
- JSON 解析失败时使用多级 fallback：严格解析 → 转义换行重试 → `_loose_extract_experiences()` 宽松正则
- per-segment 并发用 `concurrent.futures.ThreadPoolExecutor`（`max_workers` 可配置）
- `ExperiencePool` 的 `save()` / `load()` 使用 `indent=2` 人可读 JSON

## 修改流程

1. 修改 prompt → 编辑 `config.py` 中对应常量
2. 修改解析逻辑 → 编辑 `extractor.py` 中 `QiMengLogParser` / `TaskSegment`
3. 修改合并策略 → 编辑 `manager.py` 中 `ExperienceManager`；修改质量打分 → 编辑 `quality.py`；修改后处理规则 → 编辑 `postprocessor.py`
4. 新增 CLI 参数 → 编辑 `cli.py`，同步更新 README.md
5. 任何变更后检查 `__init__.py` 的导出列表是否需要更新

## 高风险区域

- `SEGMENT_EXTRACTION_PROMPT` / `EXPERIENCE_MERGE_PROMPT`：prompt 微调直接影响提取质量，改动后必须端到端验证
- `_compute_priority()` 的打分规则：被 segment 排序和日志输出引用，改动需同步检查 `render_for_prompt()`
- `_parse_llm_response()` 的 fallback 链：增删 fallback 层可能引入静默解析错误
- `ExperiencePool.save()` 的 JSON 格式：QiMeng-Agent 的 `experience_retriever.py` 直接读取此文件

## 禁止事项

- **禁止** 在 `config.py` 的 prompt 模板中使用未转义的 `{` `}` — 会导致 `.format()` 报 KeyError
- **禁止** 删除 `_loose_extract_experiences()` — 它是 LLM 返回非标准 JSON 时的最后兜底
- **禁止** 更改 `experience_pool.json` 的顶层 schema（`experiences` dict + `history` list + `metadata`）— 下游 QiMeng-Agent 依赖此结构
- **禁止** 在 `LLMClient.call()` 中硬编码模型名 — 必须从 `LLMConfig` 读取
- **禁止** 在 `TaskSegment.render_for_prompt()` 中截断 question 或 final_code — 当前设计依赖完整上下文

---

## 第二轮补充（postprocessor / quality / 候选级 merge）

### 新增模块

```
trace_evolve/
  postprocessor.py  # ExperiencePostProcessor：归一化、质量过滤、组内去重
  quality.py        # quality_score(exp)：经验质量打分，供 merge 预判和 pool 排序
```

### 数据流（更新）

```
日志 → segment_tasks() → per-segment LLM 提取 → ExperiencePostProcessor.process()
  → 归一化/过滤/去重 → ExperienceManager.merge_experiences()
  → normalize_pool()（池中旧经验 category 归一化）
  → 每条新经验：_find_candidates(top_k=5) → 规则预判 REPLACE 或 LLM _merge_single()
  → _enforce_pool_limit()（按 quality_score 排序裁剪）
  → ExperiencePool.save()
```

### 关键概念

- **normalize_pool(pool)**：在 merge 前对池中所有经验的 `category` 做归一化（与 postprocessor 的 `_CATEGORY_ALIASES` 一致），确保新旧经验格式统一。
- **quality_score(exp)**：综合 evidence、task_id、solution 长度与动作词、核心 category、task-specific 惩罚、过泛惩罚、importance 等，分数越高越应保留。用于规则预判 REPLACE 和 pool limit 排序。
- **候选级 merge**：每条新经验只与同 category 下 problem Jaccard 相似度 top-5 的旧经验做局部决策（规则预判或 LLM），不再全池批量 LLM merge。
- **规则预判 REPLACE**：当 `quality_score(new) - quality_score(best_cand) >= 3` 且 problem Jaccard ≥ 0.5 时，直接 REPLACE，不调 LLM。
- **SINGLE_EXPERIENCE_MERGE_PROMPT**：单条新经验与若干候选的 LLM merge prompt，要求「更具体、更可操作、更好 evidence 的新经验优先 REPLACE/MERGE」。

### 高风险区域（新增）

- `quality_score()` 的权重与信号：被规则预判、`_enforce_pool_limit`、`get_experiences_for_prompt` 使用，改动需同步评估对 REPLACE 预判和 pool 裁剪的影响。
- `_jaccard_combined()` 与 `_dedup_threshold_for_category()`：postprocessor 去重阈值按 category 调整，改动会影响提取后经验数量。
- `SINGLE_EXPERIENCE_MERGE_PROMPT`：单条 merge 的 LLM 决策质量，改动后需端到端验证。

### 禁止事项（新增）

- **禁止** 删除规则预判 REPLACE 逻辑 — 用于解决「旧经验挡新经验」问题，避免 LLM 偏向保留旧经验。
- **禁止** 在未评估影响的情况下修改 `quality_score` 的信号或权重 — 会影响 pool limit 裁剪和 REPLACE 预判。
- **禁止** 在 merge 前跳过 `normalize_pool()` — 池中旧经验 category 可能与 postprocessor 输出不一致，导致候选匹配失败。
